{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "aptPackages": ["default-libmysqlclient-dev"],
    "startCommand": "scripts/railway-start.sh",
    "prepareCommand": "cd src && python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py rebuild_search"
  }
}
