{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "aptPackages": ["default-libmysqlclient-dev"],
    "prepareCommand": "cd /app/src && python manage.py migrate --noinput && python manage.py collectstatic --noinput && python manage.py rebuild_search",
    "startCommand": "/app/scripts/railway-start.sh"
  }
}
